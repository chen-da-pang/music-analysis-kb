# Copy this file outside the repository, for example to:
#   ~/.config/music-kb/peers.toml
# Then replace every placeholder. Do not commit the copied file.

version = 2

[defaults]
# The publisher uses Python's standard hashlib/sqlite3 modules remotely to
# verify and atomically switch the local snapshot. Colleagues do not need a
# globally registered music-kb executable for SSH distribution.
target_dir = "~/.music-kb"
python_path = "python3"
port = 22
connect_timeout_seconds = 10
command_timeout_seconds = 600
identity_file = "~/.ssh/music-kb-sync"

[[peers]]
name = "first-colleague"
enabled = true
host = "192.0.2.42" # Documentation-only address. Use a private local config.
user = "macos-user"

[[peers]]
name = "temporarily-disabled-colleague"
enabled = false
host = "192.0.2.43"
user = "macos-user"
